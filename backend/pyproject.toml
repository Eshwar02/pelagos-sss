[project]
name = "pelagos-sss-backend"
version = "0.1.0"
description = "pelagos-sss — Indian side-scan sonar marine-debris intelligence API (SIH 2026)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.9",
    "pydantic-settings>=2.4",
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "httpx>=0.27",
]

[tool.uv]
package = false
